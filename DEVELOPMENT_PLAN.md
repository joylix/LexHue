# LexHue 重构计划（多用户版，按单机版体验重建）

## 1. 目标

基于原单机版 LexHue 的产品体验和业务规则，重新构建一个全新的 LexHue 项目，目标是：

- 在“页面布局、按钮、配色、交互方式”上尽量与原单机版保持一致；
- 在“核心业务逻辑”上尽量与原单机版保持一致；
- 在此基础上增加多用户支持，避免把多用户能力硬塞进原来的产品结构中；
- 后端如有必要再做结构化优化，但不得以“现代化”为由改写原有规则。

## 2. 设计原则

1. 先复刻，再扩展
   - 优先保证“看起来像原版、用起来像原版”，再考虑多用户隔离和账号体系。

2. 单机版为基准
   - 以原单机版的页面结构、交互细节和规则逻辑作为第一优先级参考。
   - 以 Le_mono 目录中的旧版本作为参考资料，不直接沿用旧版的“改造痕迹”。

3. 多用户是外壳能力，不是业务重写
   - 用户登录、数据隔离、权限控制这些属于外层能力；
   - 学习流程、词汇管理、测评逻辑、陌生度规则等仍然以原单机版逻辑为核心。

4. 优先保真，而不是先追求抽象
   - 后端结构可以优化，但不能破坏原始业务规则。
   - 如果某个组件没必要重写，就不要为了“更现代”而重构。

## 3. 参考来源

- 原单机版 LexHue 的页面与交互体验；
- 原单机版的学习流程与业务规则；
- Le_mono 目录中保留下来的旧版实现和文档；
- 现有 LexHuePro 里的多用户思路可作为“对接层”参考，但不作为主导设计。

## 4. 项目范围

### 4.1 前端

重点复刻：
- 页面布局：左侧导航、顶部栏、内容区、卡片式页面结构；
- 按钮风格：原版按钮的尺寸、间距、颜色、状态样式；
- 配色与视觉层级：尽量保持原单机版的色彩基调；
- 交互细节：打开/关闭、列表展示、弹窗、提示、空状态等。

### 4.2 后端

重点保留：
- 文章导入与保存；
- 词汇识别与陌生度计算；
- 逐档复习逻辑；
- 水平测评逻辑；
- 用户设置与学习进度；
- 导出/恢复等能力。

新增能力：
- 多用户登录与账号体系；
- 用户数据隔离；
- 会话和权限管理。

## 5. 重构路线

### Phase 0：项目初始化

- 创建新的前后端工程结构；
- 确认前端框架与后端运行时；
- 建立统一的开发与测试脚本；
- 先把项目跑起来，再进入功能迁移。

### Phase 1：先把页面骨架搭出来

目标：先把“长什么样”做对。

- 复刻原单机版的整体页面布局；
- 复刻主导航、子页面入口、顶部栏、内容区结构；
- 先把静态页面和样式搭好，不急着接业务逻辑。

### Phase 2：迁移核心学习流程

目标：先把“怎么用”做对。

- 文章导入与展示；
- 词汇识别与词库管理；
- 逐档复习；
- 词汇陌生度与颜色分级；
- 设置与偏好配置。

### Phase 3：严格复刻测评逻辑

目标：把测评逻辑真正做成“像单机版”。

- 默认从 4 级开始；
- 文章级别与陌生词量反馈；
- 用户根据文章难度判断“太简单 / 刚好 / 太难”；
- 根据反馈调整后续文章等级；
- 最终确定用户等级并记录设置。

### Phase 4：增加多用户能力

目标：在不破坏原体验的前提下增加账户与数据隔离。

- 用户注册/登录；
- 用户信息与设置表；
- 文章、词汇、测评会话与用户绑定；
- 数据隔离与会话管理。

### Phase 5：打磨与稳定性

目标：把体验和质量补齐。

- 修复交互细节；
- 优化错误提示与加载状态；
- 做一轮完整的端到端测试；
- 确保页面、逻辑和数据流都稳定。

## 6. 数据模型方向

建议以“单机版业务模型”为主轴，增加“用户维度”即可：

- users：账号信息；
- user_settings：用户设置与当前等级；
- articles：用户文章；
- user_vocab：用户词汇与陌生度；
- level_test_sessions：测评会话；
- annotations：文章批注；
- export/import：导入导出相关数据。

注意：数据库结构可以基于单机版规则重新组织，但不要把业务规则改成完全不同的模型。

## 7. 交付标准

本次重构完成后，至少满足以下要求：

- 前端页面和原单机版在视觉与交互上高度接近；
- 核心学习流程可正常使用；
- 水平测评逻辑与原单机版基本一致；
- 支持多用户登录与隔离数据；
- 能够顺畅执行常用业务路径。

## 8. 建议的开发顺序

1. 先做前端静态界面；
2. 再补核心业务流程；
3. 再接入多用户数据模型；
4. 最后做测试与细节收口。

## 9. 备注

这次不是“继续修补旧版本”，而是“基于单机版重新起一个真正像原版的多用户项目”。

如果后续开发过程中发现某些单机版设计不适合直接移植到多用户环境中，应优先做“最小改动兼容”，而不是改掉原始体验。
